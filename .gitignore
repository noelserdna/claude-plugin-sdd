# Claude Code local settings
.claude/

# Accidental files
nul

# Dashboard screenshots (generated, not source)
dashboard-*.png

# Playwright MCP cache
.playwright-mcp/

# MCP Server dependencies (dist/ is committed for plugin distribution)
server/node_modules/
server/package-lock.json
