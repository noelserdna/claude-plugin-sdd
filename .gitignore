# Claude Code local settings
.claude/

# Accidental files
nul

# Dashboard screenshots (generated, not source)
dashboard-*.png

# Playwright MCP cache
.playwright-mcp/

# MCP Server: node_modules/ and dist/ ARE committed for plugin distribution
# (users installing the plugin don't need to run npm install or npm run build)
